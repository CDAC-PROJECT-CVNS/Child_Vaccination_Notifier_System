import {useEffect,useState} from 'react';import {toast} from 'react-toastify';import api,{dataOf} from '../services/api';export default function SchedulePage(){const[children,setChildren]=useState([]),[childId,setChildId]=useState(''),[rows,setRows]=useState([]);useEffect(()=>{api.get('/children').then(r=>{const x=dataOf(r);setChildren(x);if(x[0])setChildId(x[0].id)})},[]);const load=()=>childId&&api.get(`/vaccinations/child/${childId}`).then(r=>setRows(dataOf(r)));useEffect(()=>{load()},[childId]);const done=async id=>{try{await api.put(`/vaccinations/child/${childId}`,{vaccineId:id,status:'COMPLETED',completedDate:new Date().toISOString().slice(0,10),notes:'Marked completed by parent'});toast.success('Vaccination marked as completed');load()}catch(x){toast.error(x.response?.data?.message||'Unable to update')}};return <div><div className="mb-5 flex justify-between gap-3"><h2 className="text-2xl font-bold">Vaccination Schedule</h2><select className="input max-w-xs" value={childId} onChange={e=>setChildId(e.target.value)}>{children.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="overflow-x-auto rounded-2xl bg-white"><table className="w-full text-left"><thead className="bg-slate-50"><tr><th className="p-4">Vaccine</th><th>Dose</th><th>Due date</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map(r=><tr className="border-t" key={r.vaccineId}><td className="p-4 font-medium">{r.vaccineName}</td><td>{r.doseNumber}</td><td>{r.dueDate}</td><td><span className={`badge ${r.status==='COMPLETED'?'bg-emerald-100 text-emerald-700':r.status==='MISSED'?'bg-red-100 text-red-700':'bg-amber-100 text-amber-700'}`}>{r.status}</span></td><td>{r.status!=='COMPLETED'&&<button className="text-emerald-700" onClick={()=>done(r.vaccineId)}>Mark completed</button>}</td></tr>)}</tbody></table></div></div>}
